import React from 'react';

     const FilePreview = ({ openState, title, onCancel, previewImage }: { openState: boolean; title: string; onCancel: () => void; previewImage: string }) => {
       return null;
     };

     export default FilePreview;